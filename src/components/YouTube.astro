---
export interface Props {
	id: string;
	alt: string;
}

const { id, alt } = Astro.props;

const style = `background-image: url('/assets/img/trailer-img.png');`;
const url = `https://youtube.com/watch?v=${id}`;

---
<link rel="stylesheet" href="/node_modules/lite-youtube-embed/src/lite-yt-embed.css" />
<script type="module" src="/node_modules/lite-youtube-embed/src/lite-yt-embed.js"></script>
<lite-youtube videoid={id} style={style} params="modestbranding=1&rel=0">
<!-- comment needed here for some reason -->
</lite-youtube>

<style>
	lite-youtube {
		max-width: unset;
		transform: scale(1.2);
		margin: 3em auto 5.5em auto;
	}

	lite-youtube::before {
		display: none;
	}

	@media screen and (max-width: 1000px) {
		lite-youtube {
			transform: unset;
			margin: 0.75em auto 2.5em auto;
		}
	}
</style>
