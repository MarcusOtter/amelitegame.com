---
export interface Props {
	text: string;
	iconUrl: string;
	linkUrl: string;
}

const { text, iconUrl, linkUrl } = Astro.props;

---

<a href={linkUrl}>
	<img src={iconUrl} alt="" role="presentation" width="24" height="24" />
	<span>{text}</span>
</a>
<style>
	a {
		padding: 0.75em 1.25em;
		background-color: #444;
		color: #fff;
		text-decoration: none;
		border-radius: 4px;
		display: flex;
		align-items: center;
		width: fit-content;
		justify-content: space-between;
		text-transform: uppercase;
	}

	a:hover,
	a:focus {
		background-color: #666;
	}

	img {
		height: 1em;
		margin-right: 0.75em;
		width: auto;
	}

	@media screen and (max-width: 500px) {
		a {
			width: 100%;
		}
	}
</style>
